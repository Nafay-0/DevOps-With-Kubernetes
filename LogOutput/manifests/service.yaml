apiVersion: v1
kind: Service
metadata:
  name: logoutput-service
  namespace: exercises
spec:
  type: ClusterIP
  selector:
    app: hashgenerator
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
